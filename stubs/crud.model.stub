<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Model;

class {{model}} extends Model
{
    {{tableName}}
    protected $fillable = [{{fillable}}, 'deleted'];
    protected $displayLabels = [{{displayLabels}}];

    // protected $casts = [{{casts}}];

{{relationships}}
}
